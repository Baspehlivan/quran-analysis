# Optional notebooks

The notebooks use only the public API or files produced by equivalent CLI commands; they contain no corpus or saved result output. Validate them as JSON with Python. Jupyter, pandas, and plotting libraries are optional local tooling and are not core runtime dependencies.
